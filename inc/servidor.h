#ifndef SERVIDOR_H
#define SERVIDOR_H

// Include any necessary headers

// Declare any classes, structs, or functions
/**
 * @brief This function represents the server functionality.
 *
 * It creates a socket, binds it to a random port, sets it to broadcast mode,
 * and sends messages to all hosts in the subnet.
 *
 * @param None
 * @return None
 */
void servidor();

#endif // SERVIDOR_H
